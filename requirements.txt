fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2
python-dotenv==1.0.1

# Phase 2 — DB (uncomment when ready)
# sqlalchemy==2.0.30
# asyncpg==0.29.0
# alembic==1.13.1

# Phase 3 — Auth (uncomment when ready)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4

# Phase 4 — SQS (uncomment when ready)
# boto3==1.34.0